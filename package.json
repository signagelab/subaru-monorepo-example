{
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "name": "monorepo-vercel-example",
  "scripts": {
    "build": "wsrun --stages --report --fast-exit --exclude-missing build && yarn workspace @signagelab/cms copy-modules-to-public-folder"
  },
  "dependencies": {
    "wsrun": "^5.2.4"
  }
}
